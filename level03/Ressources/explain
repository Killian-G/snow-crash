Pour ce niveau si on lance l'exectuable, on obtient le message suivant:

Exploit me

On peut lancer la commande `strings` sur l'exectuable pour voir toutes les chaines de caracteres contenues dans l'executable:

-----

strings ./level03 | grep "Exploit me"

-----

On obtient le message suivant:

/usr/bin/env echo Exploit me

Pour compprendre quelle fonction est utilisé dans l'executable, on peut lancer la commande `nm` sur l'executable:

-----

nm ./level03

-----

On obtient le message suivant:

...
system@@GLIBC_2.0
...

Ce qui correspond a la fonction `system()` qui permet de executer des commandes dans un shell.

On peut déduire que la fonction `system()` est utilisé dans l'executable pour afficher le message `Exploit me`.

En executant la commande `ls -l ./level03` on obtient le message suivant:

-rwsr-sr-x 1 flag03 level03 8627 Mar  5  2016 level03

le `s` dans les droits signifie que quand l'executable est executer il sera exécuter en tant `flag03` qui est le propriété de `level03`.

On peut donc essyer d'exploiter la commande `echo` executé dans `./level03` pour executer une autre commande en tant que `flag03`.

Pour faire cela on modifi la variable d'enviorment `PATH` pour qu'elle pointe vers un dossier avec un exectuable `echo` qui sera executer en tant que `flag03` et qui executera